# WB Tariffs Service

Сервис для автоматического получения и хранения тарифов коробов от Wildberries API.

## 📋 Описание

Этот сервис предназначен для:
- Ежечасного получения тарифов коробов через Wildberries API
- Хранения и обновления данных о тарифах в PostgreSQL базе данных
- Предоставления REST API для получения данных о тарифах
- Мониторинга состояния системы

## 🚀 Функциональность

### Основные возможности:
- **Автоматическое получение данных**: Ежечасное обновление тарифов через WB API
- **Накопление данных**: Сохранение исторических данных о тарифах
- **API для доступа**: RESTful API для получения тарифов по дате
- **Мониторинг**: Эндпоинты для проверки состояния системы
- **Обработка ошибок**: Устойчивость к сбоям API и сетевым проблемам

### Структура данных:
- **Метаданные тарифов**: Даты действия тарифов
- **Тарифы складов**: Детальная информация по каждому складу
- **Временные метки**: Отслеживание создания и обновления записей

## 🛠 Технологический стек

- **Node.js** + **TypeScript**
- **Express.js** - веб-фреймворк
- **PostgreSQL** - база данных
- **Knex.js** - SQL query builder и миграции
- **Axios** - HTTP клиент
- **node-cron** - планировщик задач
- **Docker** - контейнеризация

## 📦 Установка и запуск

### Требования
- Node.js >= 18.0.0
- Docker и Docker Compose
- PostgreSQL (если запуск без Docker)

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd wb-tariffs-service
```

### 2. Настройка переменных окружения
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

### 3. Запуск с Docker (рекомендуется)
```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Остановка сервисов
docker-compose down
```

### 4. Запуск без Docker
```bash
# Установка зависимостей
npm install

# Сборка проекта
npm run build

# Запуск миграций
npm run migrate

# Запуск в режиме разработки
npm run dev

# Запуск в продакшн режиме
npm start
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# База данных
DB_HOST=localhost
DB_PORT=5433
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# Wildberries API
WB_API_KEY=your_api_key_here
ENDPOINT=https://common-api.wildberries.ru/api/v1/tariffs/box

# Сервер
PORT=3333
NODE_ENV=development

# Логирование
LOG_LEVEL=INFO

# Настройки задач
MAX_CONSECUTIVE_FAILURES=5
TARIFF_DATA_RETENTION_DAYS=90
```

### Получение API ключа Wildberries
1. Войдите в личный кабинет продавца WB
2. Перейдите в раздел "Настройки" → "Доступ к API"
3. Создайте новый токен с правами на получение тарифов
4. Скопируйте токен в переменную `WB_API_KEY`

## 🔌 API Эндпоинты

### Получение тарифов по дате
```http
GET /api/tariffs?date=2024-01-15
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "meta": {
      "id": 1,
      "dt_next_box": "2024-01-15",
      "dt_till_max": "2024-03-15",
      "created_at": "2024-01-15T10:00:00Z",
      "updated_at": "2024-01-15T10:00:00Z"
    },
    "tariffs": [
      {
        "id": 1,
        "warehouse_name": "Коледино",
        "geo_name": "Центральный федеральный округ",
        "box_delivery_base": 48.00,
        "box_delivery_coef_expr": 160.00,
        "box_delivery_liter": 11.20
        // ... остальные поля
      }
    ]
  },
  "count": 15
}
```

### Получение актуальных тарифов
```http
GET /api/tariffs/latest
```

### Получение списка складов
```http
GET /api/warehouses
```

### Принудительное обновление тарифов
```http
POST /api/tariffs/refresh
```

### Проверка состояния системы
```http
GET /api/status
```

### Проверка здоровья сервиса
```http
GET /health
```

## 📊 Структура базы данных

### Таблица `tariff_meta`
| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| dt_next_box | DATE | Дата начала действия тарифов |
| dt_till_max | DATE | Дата окончания действия тарифов |
| created_at | TIMESTAMP | Время создания записи |
| updated_at | TIMESTAMP | Время последнего обновления |

### Таблица `warehouse_tariffs`
| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| tariff_meta_id | INTEGER | Внешний ключ на tariff_meta |
| warehouse_name | VARCHAR | Название склада |
| geo_name | VARCHAR | Географическое название |
| box_delivery_base | DECIMAL | Базовая стоимость доставки коробов |
| box_delivery_coef_expr | DECIMAL | Коэффициент доставки коробов |
| box_delivery_liter | DECIMAL | Стоимость за литр доставки коробов |
| box_delivery_marketplace_base | DECIMAL | Базовая стоимость доставки маркетплейс |
| box_delivery_marketplace_coef_expr | DECIMAL | Коэффициент доставки маркетплейс |
| box_delivery_marketplace_liter | DECIMAL | Стоимость за литр доставки маркетплейс |
| box_storage_base | DECIMAL | Базовая стоимость хранения |
| box_storage_coef_expr | DECIMAL | Коэффициент хранения |
| box_storage_liter | DECIMAL | Стоимость за литр хранения |
| created_at | TIMESTAMP | Время создания записи |
| updated_at | TIMESTAMP | Время последнего обновления |

## 🕐 Планировщик задач

Система автоматически выполняет следующие задачи:

- **Ежечасно**: Получение актуальных тарифов от WB API
- **При запуске**: Немедленное получение данных для инициализации
- **При ошибках**: Повторные попытки с экспоненциальной задержкой

### Настройка расписания
Cron выражение для ежечасного выполнения: `0 * * * *`

Можно изменить в файле `src/job/tariff.job.ts`:
```typescript
nodeCron.schedule("0 * * * *", async () => {
  // Ваша логика
});
```

## 📈 Мониторинг и логирование

### Уровни логирования
- **ERROR**: Критические ошибки
- **WARN**: Предупреждения
- **INFO**: Информационные сообщения
- **DEBUG**: Отладочная информация

### Метрики системы
Эндпоинт `/api/status` предоставляет:
- Состояние планировщика задач
- Время последнего успешного обновления
- Количество последовательных неудач
- Использование памяти
- Время работы сервера

## 🔧 Разработка

### Структура проекта
```
src/
├── client/          # HTTP клиенты для внешних API
├── config/          # Конфигурация приложения
├── controllers/     # HTTP контроллеры
├── job/             # Планировщик задач
├── knex/            # База данных и миграции
│   ├── migrations/  # Миграции схемы БД
│   └── seeds/       # Тестовые данные
├── repositories/    # Репозитории для работы с БД
├── services/        # Бизнес-логика
├── types/           # TypeScript типы
└── utils/           # Утилиты
```

### Команды разработки
```bash
# Разработка с hot reload
npm run dev

# Сборка проекта
npm run build

# Запуск в продакшн
npm start

# Миграции БД
npm run migrate

# Линтинг
npm run lint
```

### Создание новой миграции
```bash
npx knex migrate:make migration_name --knexfile src/knex/knexfile.ts
```

## 🐛 Отладка

### Типичные проблемы и решения

1. **Ошибка подключения к API WB**
   - Проверьте правильность API ключа
   - Убедитесь в наличии интернет соединения
   - Проверьте лимиты запросов WB API

2. **Ошибка подключения к БД**
   - Проверьте параметры подключения в .env
   - Убедитесь что PostgreSQL запущен
   - Проверьте права доступа пользователя БД

3. **Высокое потребление памяти**
   - Проверьте объем данных в БД
   - Рассмотрите настройку ротации старых данных

### Логи
```bash
# Docker логи
docker-compose logs -f app

# Логи конкретного контейнера
docker logs wb-tariffs-service_app_1 -f
```

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Для вопросов и поддержки создайте issue в репозитории или свяжитесь с командой разработки.

## 📚 Полезные ссылки

- [Документация WB API](https://dev.wildberries.ru/)
- [Knex.js документация](https://knexjs.org/)
- [Express.js документация](https://expressjs.com/)
- [Docker Compose](https://docs.docker.com/compose/)